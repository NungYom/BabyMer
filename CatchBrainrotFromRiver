-- [[ 1. UI SETUP: ปรับปรุงให้รองรับ Mouse Click บน PC (Xeno) ]]
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FishingAssist_PC_Fix"
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.DisplayOrder = 999 -- ดันให้อยู่บนสุดเพื่อรับคลิกได้แน่นอน

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 200, 0, 100)
Frame.Position = UDim2.new(0.05, 0, 0.4, 0)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.Active = true -- ต้องเป็น True เพื่อให้ Draggable ทำงาน
Frame.Draggable = true
Frame.Parent = ScreenGui

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0.9, 0, 0.5, 0)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.1, 0)
ToggleBtn.Text = "AUTO REEL: OFF"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextScaled = true
ToggleBtn.AutoButtonColor = true -- เพิ่มเพื่อให้เห็นว่ามีการกด
ToggleBtn.Parent = Frame

local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(0.9, 0, 0.3, 0)
StatusText.Position = UDim2.new(0.05, 0, 0.65, 0)
StatusText.Text = "Waiting for Cast..."
StatusText.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusText.BackgroundTransparency = 1
StatusText.TextScaled = true
StatusText.Parent = Frame

-- [[ 2. SERVICES & LOGIC (คงเดิมจาก v9 ที่ทำงานได้ดี) ]]
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local fishRemote = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("FishingService"):WaitForChild("RE"):WaitForChild("remote")

getgenv().AutoReel = false
getgenv().CastPos = nil
getgenv().TargetUUID = nil

-- Hook สำหรับจับพิกัด
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    if not checkcaller() and self == fishRemote and method == "FireServer" then
        local data = args[1]
        if type(data) == "table" and data.kind == "requestCast" and data.targetPosition then
            getgenv().CastPos = Vector3.new(data.targetPosition.X, data.targetPosition.Y, data.targetPosition.Z)
            getgenv().TargetUUID = nil
            StatusText.Text = "Cast Pos Captured!"
        end
    end
    return oldNamecall(self, ...)
end)

-- Main Loop
task.spawn(function()
    while task.wait(0.05) do
        if getgenv().AutoReel and getgenv().CastPos then
            if not getgenv().TargetUUID then
                local closestDist = 40
                for _, obj in ipairs(Workspace:GetChildren()) do
                    local extractedUUID = string.match(obj.Name, "%x%x%x%x%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%x%x%x%x%x%x%x%x")
                    if extractedUUID then
                        local mesh = obj:FindFirstChild("Mesh")
                        if mesh and mesh:IsA("BasePart") then
                            local dist = (mesh.CFrame.Position - getgenv().CastPos).Magnitude
                            if dist < closestDist then
                                closestDist = dist
                                getgenv().TargetUUID = extractedUUID
                            end
                        end
                    end
                end
            else
                -- Spam Remote ตามโครงสร้างที่คุณต้องการ
                fishRemote:FireServer({kind = "requestReel", uuid = getgenv().TargetUUID})
                fishRemote:FireServer({kind = "requestCriticalReel", uuid = getgenv().TargetUUID})
            end
        end
    end
end)

-- [[ 3. IMPROVED CLICK LOGIC: ใช้ MouseButton1Down แทน Click เพื่อความชัวร์บน PC ]]
ToggleBtn.MouseButton1Down:Connect(function()
    getgenv().AutoReel = not getgenv().AutoReel
    if getgenv().AutoReel then
        ToggleBtn.Text = "AUTO REEL: ON"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
    else
        ToggleBtn.Text = "AUTO REEL: OFF"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
        getgenv().CastPos = nil
        getgenv().TargetUUID = nil
        StatusText.Text = "Waiting for Cast..."
    end
end)
