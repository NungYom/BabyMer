local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local VIM = game:GetService("VirtualInputManager")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local Window = Rayfield:CreateWindow({
   Name = "Ultimate Farmer | v6 Pro SmartEquip",
   LoadingTitle = "Loading Combat Systems...",
   LoadingSubtitle = "by Gemini AI",
})

local MainTab = Window:CreateTab("Auto Hunter", 4483362458)
local SquareTab = Window:CreateTab("Square Loop Farm", 4483362458)
local OtherTab = Window:CreateTab("Other", 4483362458)

-- Config Shared
local AllyList = {"Survivor", "Scavenger", "Policeman", "Fighter", "Explorer", "Medic", "Farmer", "Fragger", "Guardian", "Engineer", "Hunter", "Assassin", "Soldier", "Berserker", "Necromancer", "Commander", "Psychic", "Merchant"}

-- State & Variables
local SafeHealth1, SafeHealth2 = 60, 60
local DetectionRange1 = 50 
local CenterPosHunter = Vector3.new(0, 7, 0)
local HunterEnabled, SquareEnabled = false, false
local FocusBloater = false 
local AutoEquipEnabled = false

-- Weapon Selection Variables
local SelectedMelee = ""
local SelectedRanged = ""
local LastPressTime = 0

local IsEating1, IsEating2 = false, false
local EatTimer1, EatTimer2 = 0, 0
local StartPosHunter, StartPosSquare = nil, nil
local TargetYLevel = -7
local LoopIndex = 1
local LoopPoints = {
    Vector3.new(0, 0, 50), Vector3.new(0, 0, -50),
    Vector3.new(50, 0, -50), Vector3.new(-50, 0, 50)
}

-- ฟังก์ชันช่วยดึงรายชื่ออาวุธจาก Backpack
local function GetToolList()
    local tools = {}
    for _, tool in pairs(LocalPlayer.Backpack:GetChildren()) do
        if tool:IsA("Tool") then
            table.insert(tools, tool.Name)
        end
    end
    -- เพิ่มอาวุธที่ถืออยู่ในมือปัจจุบันด้วย (ถ้ามี)
    local charFolder = workspace:FindFirstChild("Characters")
    local myChar = charFolder and charFolder:FindFirstChild(LocalPlayer.Name)
    if myChar then
        for _, tool in pairs(myChar:GetChildren()) do
            if tool:IsA("Tool") and not table.find(tools, tool.Name) then
                table.insert(tools, tool.Name)
            end
        end
    end
    return tools
end

-- ระบบสลับอาวุธแบบ Smart (เช็คจาก Workspace จริง)
local function HandleAutoEquip(root, hum)
    if not AutoEquipEnabled or not root or not hum then return end
    
    local charFolder = workspace:FindFirstChild("Characters")
    local myChar = charFolder and charFolder:FindFirstChild(LocalPlayer.Name)
    if not myChar then return end

    local y = root.Position.Y
    local isInSafeZone = (y >= -22 and y <= -18)
    
    -- เลือกอาวุธและปุ่มตามเงื่อนไข Y
    local targetWeapon = isInSafeZone and SelectedRanged or SelectedMelee
    local targetKey = isInSafeZone and Enum.KeyCode.Four or Enum.KeyCode.Two

    -- ถ้าไม่ได้เลือกอาวุธไว้ ให้ข้ามไป
    if targetWeapon == "" or targetWeapon == nil then return end

    -- เช็คว่าในตัวละคร (Workspace) มีอาวุธชิ้นนั้นอยู่หรือไม่
    local isHolding = myChar:FindFirstChild(targetWeapon)

    if not isHolding then
        -- ถ้ายังไม่ได้ถือ ให้กดปุ่มทุกๆ 1 วินาที
        if tick() - LastPressTime >= 1 then
            VIM:SendKeyEvent(true, targetKey, false, game)
            task.wait(0.05)
            VIM:SendKeyEvent(false, targetKey, false, game)
            LastPressTime = tick()
        end
    end
    
    -- Anti-Ragdoll: ล็อคตัวเมื่ออยู่โซนปลอดภัย (Y -18 ถึง -22)
    hum.AutoRotate = not isInSafeZone
end

-- ==========================================
-- หมวด 1: Auto Hunter
-- ==========================================
local CenterLabel = MainTab:CreateLabel("จุดศูนย์กลางปัจจุบัน: 0, 7, 0")

MainTab:CreateButton({
   Name = "กำหนดจุดนี้เป็นจุดศูนย์กลาง (Fix Y -7)",
   Callback = function()
      local char = LocalPlayer.Character
      local root = char and char:FindFirstChild("HumanoidRootPart")
      if root then
         CenterPosHunter = Vector3.new(root.Position.X, -7, root.Position.Z)
         CenterLabel:SetText(string.format("พิกัดศูนย์กลาง: %.1f, -7.0, %.1f", CenterPosHunter.X, CenterPosHunter.Z))
      end
   end,
})

MainTab:CreateToggle({
   Name = "โหมดโฟกัสตัวระเบิด (Focus Bloater)",
   CurrentValue = false,
   Callback = function(v) FocusBloater = v end,
})

MainTab:CreateSlider({
   Name = "ระยะการตรวจจับ (Studs)",
   Range = {0, 100}, Increment = 1, Suffix = " studs", CurrentValue = 50,
   Callback = function(v) DetectionRange1 = v end,
})

MainTab:CreateSlider({
   Name = "ตั้งค่าเลือดขั้นต่ำ",
   Range = {10, 100}, Increment = 1, Suffix = " HP", CurrentValue = 60,
   Callback = function(v) SafeHealth1 = v end,
})

MainTab:CreateToggle({
   Name = "เริ่มระบบ Auto Hunter",
   CurrentValue = false, Flag = "HunterToggle", 
   Callback = function(Value)
      HunterEnabled = Value
      if Value then
         local char = LocalPlayer.Character
         local root = char and char:FindFirstChild("HumanoidRootPart")
         if root then StartPosHunter = root.Position end
      end
      
      task.spawn(function()
         local lastSwitch, lockedEnemy = 0, nil
         while HunterEnabled do
            local char = LocalPlayer.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChild("Humanoid")
            local charFolder = workspace:FindFirstChild("Characters")
            local targetAttr = charFolder and charFolder:FindFirstChild(LocalPlayer.Name)

            if root and hum and targetAttr and StartPosHunter then
                HandleAutoEquip(root, hum)
                local hunger = targetAttr:GetAttribute("Hunger") or 100
                
                local explodingBloater = false
                local targetBloater = nil
                
                for _, obj in pairs(charFolder:GetChildren()) do
                    if obj.Name == "Bloater" then
                        local bRoot = obj:FindFirstChild("HumanoidRootPart")
                        if bRoot then
                            local dist = (root.Position - bRoot.Position).Magnitude
                            local isExploding = bRoot:GetAttribute("Speed") == nil
                            if dist <= 30 and isExploding then explodingBloater = true end
                            if FocusBloater and not isExploding and (bRoot.Position - CenterPosHunter).Magnitude <= DetectionRange1 then
                                targetBloater = bRoot
                            end
                        end
                    end
                end

                if hunger < 5 then IsEating1 = true EatTimer1 = 0
                elseif hunger >= 5 and IsEating1 then
                    if EatTimer1 == 0 then EatTimer1 = tick() + 2 end
                    if tick() >= EatTimer1 then IsEating1 = false end
                end

                local targetPoint = nil
                local isDanger = (explodingBloater or hum.Health < SafeHealth1)

                if isDanger then 
                    targetPoint = Vector3.new(CenterPosHunter.X, -20, CenterPosHunter.Z)
                elseif IsEating1 then
                    targetPoint = Vector3.new(StartPosHunter.X, -8, StartPosHunter.Z)
                elseif FocusBloater and targetBloater then
                    targetPoint = Vector3.new(targetBloater.Position.X, -7, targetBloater.Position.Z)
                else
                    if tick() - lastSwitch >= 1 then
                        local maxD, furthest = -1, nil
                        for _, obj in pairs(charFolder:GetChildren()) do
                            if not Players:GetPlayerFromCharacter(obj) and not table.find(AllyList, obj.Name) and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") and obj.Humanoid.Health > 0 then
                                local dFromCenter = (obj.HumanoidRootPart.Position - CenterPosHunter).Magnitude
                                if dFromCenter <= DetectionRange1 then
                                    local dMe = (root.Position - obj.HumanoidRootPart.Position).Magnitude
                                    if dMe > maxD then maxD = dMe furthest = obj.HumanoidRootPart end
                                end
                            end
                        end
                        lockedEnemy = furthest lastSwitch = tick()
                    end
                    if lockedEnemy and lockedEnemy.Parent then 
                        targetPoint = Vector3.new(lockedEnemy.Position.X, -7, lockedEnemy.Position.Z)
                    else targetPoint = Vector3.new(CenterPosHunter.X, -7, CenterPosHunter.Z) end
                end

                local isVertical = math.abs(root.Position.Y - targetPoint.Y) > 0.5
                local step = isVertical and 1 or (isDanger and 1 or 0.5)
                local waitTime = isVertical and 0.05 or (isDanger and 0.02 or 0.1)
                
                local dir = (targetPoint - root.Position)
                if dir.Magnitude > 0.1 then root.CFrame = CFrame.new(root.Position + dir.Unit * math.min(step, dir.Magnitude)) end
                task.wait(waitTime)
            else task.wait(0.5) end
         end
      end)
   end,
})

-- ==========================================
-- หมวด 3: Other (Selection & Smart Equip)
-- ==========================================
local MeleeDropdown = OtherTab:CreateDropdown({
   Name = "อาวุธระยะใกล้ (สล็อต 2)",
   Options = GetToolList(),
   CurrentOption = "",
   Callback = function(Option) SelectedMelee = Option end,
})

local RangedDropdown = OtherTab:CreateDropdown({
   Name = "อาวุธระยะไกล (สล็อต 4)",
   Options = GetToolList(),
   CurrentOption = "",
   Callback = function(Option) SelectedRanged = Option end,
})

OtherTab:CreateButton({
   Name = "รีเฟรชรายชื่ออาวุธ",
   Callback = function()
      local newList = GetToolList()
      MeleeDropdown:Refresh(newList)
      RangedDropdown:Refresh(newList)
   end,
})

OtherTab:CreateToggle({
   Name = "ระบบสลับอาวุธ & Anti-Ragdoll",
   CurrentValue = false,
   Callback = function(v) 
      AutoEquipEnabled = v 
      if not v then
         local char = LocalPlayer.Character
         local hum = char and char:FindFirstChild("Humanoid")
         if hum then hum.AutoRotate = true end
      end
   end,
})
