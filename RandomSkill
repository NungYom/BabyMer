local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

--==================================================
-- SERVICES & REMOTES
--==================================================
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local Remotes = ReplicatedStorage:WaitForChild("Remotes")
local SkillRE = Remotes:WaitForChild("SkillRE")
local ToolRE = Remotes:WaitForChild("ToolRE")
local FireRE = Remotes:WaitForChild("FireRE")
local FishRE = Remotes:WaitForChild("FishRE")
local SendFishRF = Remotes:WaitForChild("SendFishRF")

--==================================================
-- STATE VARIABLES
--==================================================
local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HRP = character:WaitForChild("HumanoidRootPart")
local coinCount, gemCount = 50, 50
local trading = false
local targetPlayer = nil
local targetIndex = 0

local autoAttack = false
local autoSkill = false
local huntDragon = false -- Boss07
local huntAlien = false  -- Boss08
local currentTarget = nil
local skillBusy = false

--==================================================
-- CORE LOGIC (FIXED NEAREST BOSS)
--==================================================

local function getWeapon()
    local weapon = character:FindFirstChildOfClass("Tool")
    if not weapon or not (weapon.Name:match("^Gun") or weapon.Name:match("^Harpoon")) then
        for _, v in ipairs(LocalPlayer.Backpack:GetChildren()) do
            if v.Name:match("^Gun") or v.Name:match("^Harpoon") then return v end
        end
    end
    return weapon
end

local function findTargetLogic()
    -- เจาะจง Guardian Dragon (Boss07)
    if huntDragon then
        local worldBoss = workspace:FindFirstChild("WorldBoss")
        if worldBoss then
            for _, point in ipairs(worldBoss:GetChildren()) do
                local boss = point:FindFirstChild("Boss07")
                if boss and boss:FindFirstChild("Model") and boss.Model:FindFirstChild("Root") then return boss end
            end
        end
    -- เจาะจง Alien Shark (Boss08)
    elseif huntAlien then
        local worldBoss = workspace:FindFirstChild("WorldBoss")
        if worldBoss then
            for _, point in ipairs(worldBoss:GetChildren()) do
                local boss = point:FindFirstChild("Boss08")
                if boss and boss:FindFirstChild("Model") and boss.Model:FindFirstChild("Root") then return boss end
            end
        end
    -- โจมตีตัวที่ใกล้ที่สุด (รวมทั้งปลาและบอส)
    elseif autoAttack then
        local nearest, dist = nil, math.huge
        
        -- ส่วนที่ 1: หาในทะเล (WorldSea)
        local worldSea = workspace:FindFirstChild("WorldSea")
        if worldSea then
            for _, sea in ipairs(worldSea:GetChildren()) do
                for _, fish in ipairs(sea:GetChildren()) do
                    local root = fish:FindFirstChild("Model") and fish.Model:FindFirstChild("Root")
                    if root then
                        local d = (HRP.Position - root.Position).Magnitude
                        if d < dist then dist = d nearest = fish end
                    end
                end
            end
        end

        -- ส่วนที่ 2: หาในบอส (WorldBoss) - เพิ่มกลับมาให้แล้วครับ!
        local worldBoss = workspace:FindFirstChild("WorldBoss")
        if worldBoss then
            for _, point in ipairs(worldBoss:GetChildren()) do
                for _, boss in ipairs(point:GetChildren()) do
                    local root = boss:FindFirstChild("Model") and boss.Model:FindFirstChild("Root")
                    if root then
                        local d = (HRP.Position - root.Position).Magnitude
                        if d < dist then dist = d nearest = boss end
                    end
                end
            end
        end
        return nearest
    end
    return nil
end

local function tpToCoord(x, y, z)
    HRP.CFrame = CFrame.new(x, y + 3, z)
end

local function tpToPoint(pointName)
    local worldBoss = workspace:FindFirstChild("WorldBoss")
    if worldBoss then
        local targetPoint = worldBoss:FindFirstChild(pointName)
        if targetPoint then
            HRP.CFrame = (targetPoint:IsA("BasePart") and targetPoint.CFrame or targetPoint.PrimaryPart.CFrame) * CFrame.new(0, 5, 0)
        end
    end
end

local function castSkillLoop()
    if skillBusy then return end
    skillBusy = true
    for i=1, 14 do
        if not (autoSkill or huntDragon or huntAlien) then break end
        FishRE:FireServer("Skill", {ID = string.format("Skill%02d", i)})
        task.wait(0.15)
    end
    skillBusy = false
end

--==================================================
-- UI CREATION
--==================================================
local Window = Rayfield:CreateWindow({
   Name = "👑 Fishing Hub | V12 BOSS FIX",
   LoadingTitle = "Fixing Target Logic...",
   LoadingSubtitle = "Boss Scan Re-enabled",
   ConfigurationSaving = { Enabled = false }
})

-- --- TAB: MAIN FARM ---
local TabFarm = Window:CreateTab("Main Farm", 4483362458)
TabFarm:CreateSection("Auto Hunting")
TabFarm:CreateToggle({
   Name = "โจมตีอัตโนมัติ (Nearest Fish & Boss)",
   CurrentValue = false,
   Callback = function(Value)
      autoAttack = Value
      if Value then huntDragon, huntAlien, currentTarget = false, false, nil end
   end,
})
TabFarm:CreateToggle({
   Name = "🎯 ล่า Guardian Dragon (Boss07)",
   CurrentValue = false,
   Callback = function(Value)
      huntDragon = Value
      if Value then autoAttack, huntAlien, currentTarget = false, false, nil end
   end,
})
TabFarm:CreateToggle({
   Name = "🎯 ล่า Alien Shark (Boss08)",
   CurrentValue = false,
   Callback = function(Value)
      huntAlien = Value
      if Value then autoAttack, huntDragon, currentTarget = false, false, nil end
   end,
})
TabFarm:CreateToggle({
   Name = "ออโต้สกิล (01-14)",
   CurrentValue = false,
   Callback = function(Value) autoSkill = Value end,
})

TabFarm:CreateSection("Boss Point TP")
TabFarm:CreateButton({ Name = "วาร์ปไปจุดเกิด Guardian Dragon", Callback = function() tpToPoint("Point6") end })
TabFarm:CreateButton({ Name = "วาร์ปไปจุดเกิด Alien Shark", Callback = function() tpToPoint("Point7") end })

-- --- TAB: TELEPORT ---
local TabTP = Window:CreateTab("Teleport", 4483362458)
TabTP:CreateSection("World Locations")
TabTP:CreateButton({ Name = "Island Center Lake", Callback = function() tpToCoord(1265, 87, -271) end })
TabTP:CreateButton({ Name = "Nether Island", Callback = function() tpToCoord(356, 86, -1077) end })
TabTP:CreateButton({ Name = "Under Water", Callback = function() tpToCoord(1192, -1136, 3878) end })
TabTP:CreateButton({ Name = "Lost Sea Ruins", Callback = function() tpToCoord(1222, -1115, 2976) end })

-- --- TAB: GACHA & TRADE (UNCHANGED) ---
local TabGacha = Window:CreateTab("Gacha", 4483362458)
TabGacha:CreateSlider({ Name = "จำนวนสุ่ม", Range = {1, 100}, Increment = 1, Suffix = "ครั้ง", CurrentValue = 50, Callback = function(V) coinCount = V gemCount = V end })
TabGacha:CreateButton({ Name = "สุ่มด้วยเงิน (Coins)", Callback = function() SkillRE:FireServer("SkillGacha",{Key="Coins",Count=coinCount}) end })
TabGacha:CreateButton({ Name = "สุ่มด้วยเพชร (Gems)", Callback = function() SkillRE:FireServer("SkillGacha",{Key="Gems",Count=gemCount}) end })

local TabTrade = Window:CreateTab("Trade System", 4483362458)
local PlayerLabel = TabTrade:CreateLabel("เป้าหมาย : ยังไม่ได้เลือก")
TabTrade:CreateButton({ Name = "เลือกผู้เล่นคนถัดไป", Callback = function()
      local list = {}
      for _,p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then table.insert(list,p) end end
      if #list == 0 then return end
      targetIndex = (targetIndex % #list) + 1
      targetPlayer = list[targetIndex]
      PlayerLabel:Set("เป้าหมาย : "..targetPlayer.Name)
end })
TabTrade:CreateToggle({ Name = "เริ่มเทรดอัตโนมัติ (0.5s)", CurrentValue = false, Callback = function(V)
      trading = V
      if trading and targetPlayer then
          task.spawn(function()
              while trading and targetPlayer do ToolRE:FireServer("Switch",{index=3}) SendFishRF:InvokeServer(targetPlayer) task.wait(0.5) end
          end)
      end
end })

--==================================================
-- MAIN LOOP
--==================================================
task.spawn(function()
    while true do
        local weapon = getWeapon()
        if not currentTarget or not currentTarget.Parent or not currentTarget:FindFirstChild("Model") then
            currentTarget = findTargetLogic()
        end
        if currentTarget and weapon then
            local root = currentTarget.Model:FindFirstChild("Root")
            if root then
                FireRE:FireServer("Hit", {fishInstance = currentTarget, HitPos = root.Position, toolInstance = weapon})
                if autoSkill or huntDragon or huntAlien then task.spawn(castSkillLoop) end
            end
        elseif autoSkill and not (huntDragon or huntAlien) then
            task.spawn(castSkillLoop)
        end
        task.wait(0.2)
    end
end)

Rayfield:Notify({Title = "V12 Loaded", Content = "แก้ไขระบบเล็งบอสตัวที่ใกล้ที่สุดเรียบร้อย!", Duration = 5})
