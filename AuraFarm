local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- [[ 1. สร้าง ScreenGui ]]
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FinalFarmGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

-- [[ 2. ฟังก์ชันสร้างปุ่ม ]]
local function createButton(name, pos, color)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(0, 180, 0, 45)
    btn.Position = pos
    btn.Text = name .. ": OFF"
    btn.BackgroundColor3 = color
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 18
    btn.Parent = screenGui

    local uiCorner = Instance.new("UICorner")
    uiCorner.CornerRadius = UDim.new(0, 10)
    uiCorner.Parent = btn
    
    return btn
end

local btnLock = createButton("LockPos", UDim2.new(0.8, 0, 0.4, 0), Color3.fromRGB(80, 80, 80))
local btnFlag = createButton("FlagFarm", UDim2.new(0.8, 0, 0.5, 0), Color3.fromRGB(80, 80, 80))
local btnAuto = createButton("AutoFarm", UDim2.new(0.8, 0, 0.6, 0), Color3.fromRGB(80, 80, 80))

-- [[ 3. ตัวแปรสถานะ ]]
local mode = "None"
local savedCFrame = nil

-- [[ 4. ฟังก์ชันค้นหาเป้าหมาย (แก้ไข Hitbox และ Priority) ]]
local function getTarget()
    local zones = workspace:FindFirstChild("Map") and workspace.Map:FindFirstChild("Zones")
    if not zones then return nil end

    if mode == "Lock" then
        return savedCFrame
    elseif mode == "Flag" then
        -- แก้ไข: ให้ลดระดับแกน Y ลง 2 Studs
        local flag = zones:FindFirstChild("FlagZone")
        if flag then
            return flag.CFrame * CFrame.new(0, -2, 0)
        end
    elseif mode == "Auto" then
        -- ลำดับความสำคัญ: 10X -> 4X -> 3X -> 2X
        local priorities = {"10XAura", "4XAura", "3XAura", "2XAura"}
        
        for _, auraName in ipairs(priorities) do
            local zoneFolder = zones:FindFirstChild(auraName)
            if zoneFolder then
                -- ตรวจสอบชื่อ "Hitbox" (b ตัวเล็กตามรูป Explorer)
                local hb = zoneFolder:FindFirstChild("Hitbox") or zoneFolder:FindFirstChild("HitBox")
                if hb then
                    return hb.CFrame
                end
            end
        end
        
        -- ถ้าไม่เจอ Aura เลย ให้ไปที่ FlagZone และลด Y ลง 2 Studs
        local flag = zones:FindFirstChild("FlagZone")
        if flag then
            return flag.CFrame * CFrame.new(0, -2, 0)
        end
    end
    return nil
end

-- [[ 5. ระบบ Loop TP แบบ Locked Physics ]]
RunService.Stepped:Connect(function()
    if mode == "None" then return end
    
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    
    local target = getTarget()
    if target then
        root.CFrame = target
        root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
    end
end)

-- [[ 6. ระบบ Toggle ปุ่ม ]]
local function updateUI()
    btnLock.Text = (mode == "Lock") and "LockPos: ON" or "LockPos: OFF"
    btnLock.BackgroundColor3 = (mode == "Lock") and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(80, 80, 80)
    
    btnFlag.Text = (mode == "Flag") and "FlagFarm: ON" or "FlagFarm: OFF"
    btnFlag.BackgroundColor3 = (mode == "Flag") and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(80, 80, 80)
    
    btnAuto.Text = (mode == "Auto") and "AutoFarm: ON" or "AutoFarm: OFF"
    btnAuto.BackgroundColor3 = (mode == "Auto") and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(80, 80, 80)
end

btnLock.MouseButton1Click:Connect(function()
    if mode == "Lock" then mode = "None" else
        mode = "Lock"
        local char = player.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if root then savedCFrame = root.CFrame end
    end
    updateUI()
end)

btnFlag.MouseButton1Click:Connect(function()
    mode = (mode == "Flag") and "None" or "Flag"
    updateUI()
end)

btnAuto.MouseButton1Click:Connect(function()
    mode = (mode == "Auto") and "None" or "Auto"
    updateUI()
end)
