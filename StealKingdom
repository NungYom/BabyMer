local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Plot Auto Collector (Multi-Press)",
   LoadingTitle = "Starting Script...",
   LoadingSubtitle = "Gemini AI Custom",
   ConfigurationSaving = { Enabled = false }
})

local _G = { AutoCollect = false }
local vim = game:GetService("VirtualInputManager")

-- ฟังชั่นตรวจสอบ Plot ของผู้เล่น
local function GetPlayerPlot()
    local playerName = game.Players.LocalPlayer.Name
    for _, plot in pairs(workspace.Plots:GetChildren()) do
        local ownerObj = plot:FindFirstChild("Owner")
        if ownerObj and tostring(ownerObj.Value) == playerName then
            return plot
        end
    end
    return nil
end

local Tab = Window:CreateTab("Auto Farm", 4483362458)

Tab:CreateToggle({
   Name = "Start Auto Collect (10x E-Press)",
   CurrentValue = false,
   Flag = "AutoFarm", 
   Callback = function(Value)
      _G.AutoCollect = Value
      
      if Value then
          task.spawn(function()
              while _G.AutoCollect do
                  local myPlot = GetPlayerPlot()
                  
                  if myPlot and myPlot:FindFirstChild("Building") then
                      local buildings = myPlot.Building:GetChildren()
                      
                      for _, item in pairs(buildings) do
                          if not _G.AutoCollect then break end
                          
                          local mainPart = item:FindFirstChild("MainPart")
                          if mainPart and mainPart:IsA("BasePart") then
                              -- 1. TP ไปยัง MainPart
                              game.Players.LocalPlayer.Character:SetPrimaryPartCFrame(mainPart.CFrame)
                              
                              -- 2. รอ 0.3 วินาที ให้ตัวละครนิ่ง
                              task.wait(0.3)
                              
                              -- 3. ลูปกดปุ่ม E ทั้งหมด 10 รอบ
                              for i = 1, 10 do
                                  if not _G.AutoCollect then break end
                                  
                                  vim:SendKeyEvent(true, Enum.KeyCode.E, false, game)
                                  task.wait(0.05) -- กดลง
                                  vim:SendKeyEvent(false, Enum.KeyCode.E, false, game)
                                  
                                  task.wait(0.1) -- รอ 0.1 วิ ตามโจทย์ก่อนกดรอบถัดไป
                              end
                              
                              -- 4. รอ 0.2 วินาที ก่อนจะไปที่สิ่งปลูกสร้างถัดไป
                              task.wait(0.2)
                          end
                      end
                  else
                      task.wait(1)
                  end
                  task.wait(0.1)
              end
          end)
      end
   end,
})

Rayfield:Notify({
    Title = "Script Loaded",
    Content = "ตั้งค่ากด E 10 รอบเรียบร้อยแล้ว!",
    Duration = 5
})
